name: Retired CodeAgent 3.4.19 publisher

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This one-shot release workflow is retired.'
